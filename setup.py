from distutils.core import setup
from setuptools import find_packages

setup(
    name='snowflake',
    version='0.1',
    author='Yasemin',
    author_email='yasemin.erboga@fau.edu',
    packages=find_packages(),
    install_require=['numpy', 'turtles']

)
